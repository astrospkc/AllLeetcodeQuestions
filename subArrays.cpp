#include<bits/stdc++.h>
using namespace std;

void subArrays(int i, int ptr, vector<int>& curr, vector<vector<int>>& res, vector<int>& arr){

}

int main(){
    vector<int> arr = {1,2,3,4};
    vector<int> curr;
    vector<vector<int>> res;

    for(int i= 0; i<arr.size(); i++){
        sub0
    }
}